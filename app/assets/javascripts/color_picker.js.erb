function colorPicker() {
  $('#color').minicolors();
  changeSiteColor();
  $("#color").change(function () {
    var color = this.value;
    $.ajax({
      url: "/site_color",
      type: "GET",
      data: {
        'color' : color
      }
    });
  });
}
